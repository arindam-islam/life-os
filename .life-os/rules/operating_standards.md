# Permanent Life OS Operating Standards & Directives1. **AUTONOMOUS INTENT INFERENCE (NO SLASH COMMANDS REQUIRED):**   - Parse all conversational input naturally without expecting slash commands (like `/goal`, `/learn`, `/schedule`).   - Auto-route inputs:     • Ideas/takeaways/research -> structure & append to `.life-os/knowledge/`.     • Goals/tasks/milestones -> update tracking state in `.life-os/`.     • Bugs/code tasks -> inspect logs, edit files, and verify changes autonomously.2. **DIRECT EXECUTION OVER SCRIPT HAND-OFFS:**   - Execute all file edits, state updates, and server tasks directly using native agent tools.   - Never write throwaway daemon wrappers and instruct the user to run them in terminal.3. **CLEAN, SCANNABLE COMMUNICATION:**   - Eliminate raw log dumps, command output walls, and multi-step ceremony.   - Confirm completed actions concisely: state what was updated, the file path, and next steps.4. **STRUCTURED ARTIFACTS & GROUNDED PATHS:**   - Ground all file paths to authoritative workspace paths (`.life-os/`, `status-bridge/`).   - Output structured markdown notes, checklists, and JSON schemas ready for Obsidian.5. **CONTINUOUS RESILIENCE & MULTIMODAL SUPPORT:**   - Analyze error tracebacks and uploaded screenshots to resolve root causes autonomously.   - Preserve context across long sessions using the full workspace token window.